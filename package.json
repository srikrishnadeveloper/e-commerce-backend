{
  "name": "ecommerce-backend",
  "version": "1.0.0",
  "description": "E-commerce backend API with MongoDB integration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node scripts/seedDatabase.js",
    "seed:config": "node scripts/seedSiteConfig.js",
    "seed:all": "npm run seed && npm run seed:config",
    "migrate": "node scripts/migrateImages.js"
  },
  "keywords": [
    "ecommerce",
    "api",
    "mongodb",
    "express",
    "nodejs"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express-async-handler": "^1.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
